# Environment: development or production
NODE_ENV=development

# Server
PORT=5000
...
# Server
PORT=5000

# PostgreSQL connection settings
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=@Sconzy3030
DB_NAME=care_management

# Auth
JWT_SECRET=replace_this_with_a_long_random_string
JWT_EXPIRES_IN=1d

# Initial admin account (used only by the seed script)
ADMIN_EMAIL=admin@carehome.local
ADMIN_PASSWORD=ChangeThisNow123!
ADMIN_FIRST_NAME=System
ADMIN_LAST_NAME=Admin

# Email (Resend). Leave RESEND_API_KEY empty to log invitations to console.
RESEND_API_KEY=
FROM_EMAIL=CareHome Connect <onboarding@resend.dev>
APP_URL=http://localhost:5173
INVITATION_EXPIRY_HOURS=48

# Environment: development or production
NODE_ENV=development

# Production database (Neon). Leave empty in development.
# DATABASE_URL=postgresql://neondb_owner:password@host/neondb?sslmode=require

# Local PostgreSQL connection settings
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=@Sconzy3030
DB_NAME=care_management

# ... the rest of the file stays as it is ...